# Local Maahishmati State

This directory is created by `bahubali`.

Tracked:

- `agents.json` can be committed when you want a project-specific court definition.

Ignored:

- `latest.json`
- `runs/`

Those files are local mission history.
